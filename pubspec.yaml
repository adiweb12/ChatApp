name: my_chat_app
description: "A new Flutter project for Android testing."
publish_to: 'none'

version: 2.0.0+1

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0

  # Removed cupertino_icons since we are focusing on Android/Material

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  # If you add images later, uncomment the lines below:
  # assets:
  #   - assets/images/
